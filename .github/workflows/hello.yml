name: Hello!

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello through ubuntu-latest"
      - name: Time now
        run: |
          date
      - name: Current directory content
        run: |
          ls
